# Bitácora T06 — arca-sign

---
Task ID: T06
Agent: Super Z (agente principal; primer intento de subagente quedó a medias por timeout del API — se recuperaron y adaptaron sus tests)

## Pasos
1. Leída specs/arca-08-sign.md + docs/06 §5 (formato .arca y firma).
2. Implementación completa: digest.rs (package_digest canónico P/M + longitudes prefijadas), ring.rs (RingOfTrust, formato bin [u32 n][32×n], from_embedded con fail-closed), stream.rs (StreamingVerifier con feed/end_file/finish, abort temprano por sha), sig.rs (PackageSignature ARCASIG1 = 106 B: magic+ver u16+key_id 32+sig 64), signer.rs (feature `signer`: keygen getrandom, generate_keypair 0600, sign_digest, sign_package).
3. Recuperados tests del subagente fallido (mutations.rs 219 casos, perf.rs bench 10k, prop_canonical.rs proptest) y adaptados a la API final.
4. ed25519-dalek 3.0: traits Signer/Verifier requieren import explícito; rand_core 0.10 sin OsRng → getrandom 0.4 directo para el seed.

## Decisiones
- `expected` del StreamingVerifier EXCLUYE manifest.toml (auto-referencia imposible), signature.bin y manifest.digest (el installer los lee aparte por extracción selectiva; feed de cualquiera de ellos = error).
- sha256 para archivos (lo declara el manifest), blake3 para manifest (M) — mezcla deliberada según docs/06 §5.
- Enmienda spec 01: ArcaError += InvalidPackage(&'static str) (aditivo, non_exhaustive).
- Enmienda spec 08: deps += sha2; paths como &str (no RelPath) para no depender de pkg-model.
- TASKS.json "10k verif < 50ms" es inalcanzable para ed25519 (~60-90µs/op): presupuesto real = 1 verif < 5ms; bench mide y reporta ambos.

## Estado
- 32 tests verdes (20 lib + 9 mutations 219 casos + 1 perf + 2 proptest), clippy -D warnings verde, fmt aplicado.
