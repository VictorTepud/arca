---
Task ID: T11
Agent: Super Z (agente principal)
Task: arca-shm — memfd + ring SPSC + seqlock de frames (spec 05)

Work Log:
- memfd.rs: Memfd (nix memfd_create con fallback a syscall() en Android,
  MFD_CLOEXEC|MFD_ALLOW_SEALING, ftruncate, seal_size GROW|SHRINK, prefijo
  "arca-" obligatorio).
- map.rs: ShmMap RAII (MmapRaw, drop=munmap, validación len==tamaño del fd
  anti-SIGBUS).
- ring.rs: RingSpsc lock-free (header 64B cache-line, head/tail Release/
  Acquire, detector de doble productor/consumidor por VISTA con pid+contador
  — funciona cross-process tras fork), push/pop/pop_each cero-copias,
  push_compacting con contrato documentado de seq embebido por slot.
- frame.rs: FrameSlots double-buffer con seqlock — BUG DE DISEÑO real
  encontrado y corregido por el test de fork: con seqs locales por slot
  ambos slots EMPATAN (99 vs 99) tras publicaciones alternas y el lector
  elige el frame viejo → seq GLOBAL monotónico por publicación (next_seq
  privado de la instancia del escritor, re-init por launch documentado).
- WriteSlot guard: begin_write → payload → publish (Release); drop sin
  publish = frame abortado limpio (seq queda par = inválido).
- Tests: 9 lib (FIFO, overflow, compactación determinista, doble productor,
  SPSC 10M con hilos reales, torn-read del seqlock con escritor agresivo,
  publish/abort, geometría) + 3 de fork REAL (ring 1000 slots padre↔hijo
  por memfd, frames 100 publicaciones leídas por el padre, seals).

Stage Summary:
- El músculo del rendimiento está listo y probado con procesos reales.
- Invariante "sin futex en shm" por construcción (freezer-tolerante).
