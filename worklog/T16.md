---
Task ID: T16
Agent: Super Z (agente principal)
Task: arca-rt — runtime de sub-app headless + app de test ping (spec 22)

Work Log:
- lib.rs: arca_main (crash hook → log init → Conn::from_fd(4) + eventfds
  5/6 heredados → handshake_client con identidad del env ARCA_* (nonce sin
  getrandom-de-archivo: mezcla de clocks — getrandom syscall SÍ está
  permitida pero /dev/urandom NO; v1 usa clocks + pid, documentado) → mapeo
  de memfds → frame loop controlado por el host).
- Frame loop: poll {eventfd tick, ctl} 100 ms → drain del ring de input →
  catch_unwind(callback de la app) → publish_frame (seqlock + FrameReady
  por eventfd) → despacho de ctl (Ping→Pong, Shutdown→exit 0, Pause→Busy,
  ConfigChanged/WindowOpen/Close/Focus tipados).
- Exit codes: 0 (Shutdown), 101 (panic/error de app, con CrashReport
  best-effort + hash FNV del mensaje), 102 (handshake muerto). Backtrace
  simbólico IMPOSIBLE bajo el sandbox (openat bloqueado — documentado;
  minidump real = T21 con maps pre-abiertas).
- bin/ping.rs: app de test (echo Ping/Pong, frame por tick, ARCA_PING_PANIC/
  SOCKET/TICKS para los e2e de T15).
- F2 headless: FrameBuilder mínimo ([frame_seq][ticks][input_total] u64);
  MeshFrame real llega con gfx-protocol (T17) + sdk-ui (T21).

Stage Summary:
- "El libc de Arca" para las apps: el mismo ping corre hoy en PC bajo
  seccomp real y correrá en Android aarch64 sin cambios de código.
