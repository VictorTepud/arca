---
Task ID: T15
Agent: Super Z (agente principal)
Task: arca-exec-native + bin arca-launch — spawn/seccomp/monitor (spec 14)

Work Log:
- spec.rs: LaunchSpec binario propio ([magic ARCLNC1][campos][crc32]),
  determinista, roundtrip + corrupciones testeadas.
- bus.rs: ConnBus (impl BusTransport de exec-abi sobre la Conn de ipc —
  regla de huérfanos: solo un crate L1 puede unir trait L1 + tipo L0),
  bus "vacío" hasta el handshake (mensajes previos → error tipado).
- launch.rs (BIN arca-launch): lee spec del fd 3 (lectura EXACTA [len][blob]
  — sin depender de EOF porque hereda el extremo de escritura), enumera
  /proc/self/fd ANTES del filtro, build_profile+apply_profile (seccomp
  ANTES de execve: invariante spec 07 §4), cierra todo fd fuera de
  {0,1,2,4,5,6}, chdir(app_dir), execve con env mínimo (identidad ARCA_* +
  passthrough ARCA_* controlado).
- watch.rs: watcher por instancia (handshake_server → instala Conn ANTES de
  emitir Hello/Ready (carrera encontrada por el e2e y corregida) → reap
  waitpid(WNOHANG) cada 5 ms → Dead tipado) + drain de stdout/stderr a
  tracing con target por app (ring de línea 64 KB drop-el-más-viejo).
- lib.rs: NativeProcessExec (posix_spawn con dup2 fijo 3=spec/4=ctl/5=tick/
  6=ready/1-2=pipes/0=/dev/null; memfd frames+input creados e inicializados
  por el host; eventfd UNO por dirección compartiendo fd con el hijo — bug
  de pares independientes encontrado por el e2e) + NativeInstance con TODOS
  los handles (bus, tick, ready, vistas shm) para host-core/tests.
- BUG de pipes encontrado por el e2e: nix pipe2 devuelve (read, write) y
  el naming estaba invertido → fd 3 era el extremo de ESCRITURA (EBADF).
- Base de syscalls rev2 (chdir/execve/arch_prctl/set_tid_address) + tabla
  propia x86_64/aarch64 de T14 actualizada + goldens regenerados.
- E2E (musl estático para la app de prueba — openat está bloqueado por el
  sandbox: un binario dinámico moriría en el loader): spawn→handshake→
  ping RTT p99 = 46 µs (presupuesto 1 ms)→kill-9→Dead ≤ 50 ms; 100 spawns
  + shutdown limpios en 1.6 s; seccomp PROBADO (socket() → SIGSYS 31);
  panic de app → exit 101 + CrashReport; shutdown limpio → exit 0.

Stage Summary:
- El backend nativo funciona de punta a punta en PC con sandbox REAL.
- Desviaciones v1 documentadas: socketpair directa (no UDS app.sock), bus
  del ABI ignorado (la Conn nace del spawn), reap por hilo (no signalfd),
  Attach default de 1 ventana. T22 (host-core) las revisa.
