---
Task ID: T10
Agent: Super Z (agente principal)
Task: arca-protocol — mensajes AIPC v1 (rkyv) + framing + golden bytes (spec 03)

Work Log:
- arca-types: feature opt-in `rkyv` (derives Archive/Serialize/Deserialize +
  compare(PartialEq)) en AppId/InstanceId/WinId/SessionId/ProtoVersion/
  Capability/Digest; rkyv workspace con features smol_str-0_3 + unaligned
  (acceso desde Vec<u8> no alineado; determinismo de golden bytes).
- error.rs: variante nueva `InvalidFrame(&'static str)` (non_exhaustive
  permitía añadirla sin romper consumers).
- wire.rs: ControlMsg completo de docs/04 §4 (lifecycle, handshake
  Hello/Welcome/Ready/Attach, SvcRequest/Response, Ping/Pong, CrashReport,
  GrantFd) + SignalMsg (5 kinds) + structs auxiliares tipados (Size, Insets,
  Theme, ScalePm por-mil, ShmLayout, UiCaps, WindowSpec...).
- framing.rs: header 26 B (magic 0x41CA1C00, version, flags, channel, length,
  seq, crc32) + encode_into/encode_signal_into/decode/decode_signal (rkyv
  access VALIDADO, zero-alloc exit-path) + señal eventfd u64 taggeada
  (encode/decode_signal_wire, kinds 1..5 estables).
- CRC32 por segmentos (0..22 + 26..fin, campo excluido) — encoder y decoder
  con el MISMO convenio (bug de convenio mixto detectado por el propio test
  golden y corregido antes de fijar fixtures).
- Fixtures: tests/fixtures/golden_v1_0.txt (12 ctl + 5 señal, INMUTABLES;
  regenerador ARCA_PRINT_GOLDEN). Roundtrip 10k mensajes deterministas +
  stream multimensaje + overflow tipado + canal cruzado rechazado.
- fuzz-lite 200k (100k random + 100k mutaciones de tramas válidas) sin
  pánicos. 11 tests, clippy -D warnings verde.

Stage Summary:
- Protocolo AIPC v1 completo y versionado: la fuente de verdad wire está
  fijada por golden bytes; compat N-1 = añadir entradas SOLO al final.
- Desviaciones documentadas: Vec<Capability> en Welcome (no SmallVec),
  GrantFd con req_id, seq global por dirección lo pone el transporte.
