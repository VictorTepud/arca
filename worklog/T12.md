---
Task ID: T12
Agent: Super Z (agente principal)
Task: arca-ipc — UDS + SO_PEERCRED + SCM_RIGHTS + handshake + eventfd (spec 06)

Work Log:
- uds.rs: ensure_filesystem_path (abstract namespace PROHIBIDO: NUL interior
  y prefijo @ rechazados — docs/01 §4).
- conn.rs: Server::bind (stale socket idempotente, 0700 en socket y dir),
  accept(deadline) con poll, verificación SO_PEERCRED en CADA conexión
  (UID ≠ propio → rechazo); Client::connect con backoffs 5/20/100 ms ×6
  (carrera de spawn); Conn: send_frame/recv_frame con SCM_RIGHTS
  (MAX_FDS_PER_SEND, pending-fds RAII entre recvs parciales, MSG_NOSIGNAL
  anti-SIGPIPE), recv SIN set_deadline = error tipado (decisión de spec 06
  §6 elegida: runtime-check), send/recv ctl y señal por socket.
- signal.rs: SignalChannel (eventfd, wakeup PURO — decisión documentada:
  eventfd SUMA valores y dos señales acumuladas corromperían el payload
  u64; el payload real viaja por ring/slot Vsync o por socket).
- handshake.rs: handshake_server (valida app_id/instance/artifact_hash +
  versión → WELCOME con 2 memfd por SCM_RIGHTS → READY → ATTACH) y
  handshake_client (devuelve Conn + memfd vivos + attach).
- Tests: 12 (loopback 10k mensajes con 20 fds reales, handshake completo
  con memfd USABLE de punta a punta, identidad maliciosa rechazada, p99
  señal eventfd < 200 µs, abstract rechazado, timeout limpio, 0700).

Stage Summary:
- Transporte AIPC nativo completo; los memfd viajan por el socket y el
  handshake valida identidad (anti-sustitución) y versión.
