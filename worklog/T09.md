# Bitácora T09 — arca-tools-pk

---
Task ID: T09
Agent: Super Z (principal)

## Implementación
- CLI clap: keygen / graph / pack / verify / trust-ring add. Exit codes 0/1/2 documentados.
- pack: manifest+artefactos+fonts validados → graph sync (auto-gen si falta, FAIL si desincronizado) → shas → build.json regenerado → 7z LZMA2 solid-OFF orden determinista (manifest primero) → firma PackageSignature → manifest.digest + signature.bin al final. Byte-determinista (timestamps por defecto fijos del writer).
- verify: MISMO camino del host (Archive pass1 meta + pass2 VerifySink hashing + StreamingVerifier + ring 1 clave). Zero re-implementación cripto.
- graph: nodos=archivos .rs, aristas=use crate::x; mmd con archivo+label; check por normalización whitespace.
- trust-ring add: txt hex (dedup) → bin [u32 n][32×n] parseable por RingOfTrust::from_bin.
- arca-sign: push_bytes() añadido (evita exponer dalek al CLI).

## Bugs encontrados y arreglados
- keygen no creaba el directorio de salida (solo el parent) → ENOENT.
- match backend con guards caía al brazo default con bin presente.

## Tests 8/8 (pack→verify, doble pack determinista (digest+bytes), graph sync+fail+regen, 20 mutaciones ≥18 rechazo, e2e pack→verify→INSTALL→verify_installed→uninstall, CLI help/exit codes, keygen 0600, trust-ring dedup)
- e2e usa dev-dep arca-installer: la aceptación "pack→verify→install PC end-to-end" se cumple LITERAL (0.22 s).

## Desvíos
- compile-wasm no implementado (F5/T25: wamrc pineado). --backend wasm exige binario ya compilado.
- graph: análisis léxico por líneas (use crate::…), no AST completo — suficiente para el check de sincronía v1.
